<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ana.quiz"> <!-- Altere para o nome do seu pacote -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Quiz"
        tools:targetApi="31">

        <!-- Activity Principal (Pergunta 1) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Quiz - Pergunta 1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Pergunta 2 -->
        <activity
            android:name=".SecondQuestionActivity"
            android:exported="false"
            android:label="Quiz - Pergunta 2" />

        <!-- Pergunta 3 -->
        <activity
            android:name=".ThirdQuestionActivity"
            android:exported="false"
            android:label="Quiz - Pergunta 3" />

        <!-- Resultado -->
        <activity
            android:name=".ResultActivity"
            android:exported="false"
            android:label="Resultado do Quiz" />

    </application>

</manifest>